<Window x:Class="StructureHelper.Windows.Shapes.RingShapeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StructureHelper.Windows.Shapes"
        d:DataContext="{d:DesignInstance local:RingShapeViewModel}"
        mc:Ignorable="d"
        Title="Ring Shape Edit" Height="250" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="22"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Outer Diameter"/>
            <TextBox Grid.Column="1" Text="{Binding OuterDiameter, Converter={StaticResource LengthConverter}, ValidatesOnDataErrors=True}"/>
            <TextBlock Grid.Row="1" Text="Inner Diameter"/>
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding InnerDiameter, Converter={StaticResource LengthConverter}, ValidatesOnDataErrors=True}"/>
            <TextBlock Grid.Row="2" Text="Outer radius"/>
            <TextBox Grid.Column="1" Grid.Row="2" IsEnabled="False" Text="{Binding OuterRadius, Converter={StaticResource LengthConverter}, ValidatesOnDataErrors=True}"/>
            <TextBlock Grid.Row="3" Text="Inner Radius"/>
            <TextBox Grid.Column="1" Grid.Row="3" IsEnabled="False" Text="{Binding InnerRadius, Converter={StaticResource LengthConverter}, ValidatesOnDataErrors=True}"/>
        </Grid>
        <ContentControl Grid.Row="1" ContentTemplate="{StaticResource OkCancelButtons}" Content="{Binding}"/>
    </Grid>
</Window>
