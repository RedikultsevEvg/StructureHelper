<UserControl x:Class="StructureHelper.Windows.CalculationWindows.CalculatorsViews.ForceCalculatorViews.IsoFieldContours"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StructureHelper.Windows.CalculationWindows.CalculatorsViews.ForceCalculatorViews"
             d:DataContext="{d:DesignInstance local:ContoursRangeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="220">
    <Grid>
        <ScrollViewer Grid.Column="2">
            <StackPanel>
                <TextBlock Text="Minimum value"/>
                <TextBox x:Name="tbMinValue" Margin="20,3,3,3" Text="{Binding Path=UserMinValue, Converter={StaticResource PlainDouble}, ValidatesOnDataErrors=True}"/>
                <TextBlock Text="Maximum"/>
                <TextBox x:Name="tbMaxValue" Margin="20,3,3,3" Text="{Binding Path=UserMaxValue, Converter={StaticResource PlainDouble}, ValidatesOnDataErrors=True}"/>
                <!--<Button Margin="3" Content="Set custom colors" Command="{Binding SetUserColorsCommand}"/>-->
                <local:IsoFieldVerticalLegend DataContext="{Binding ContourLegend}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
