<Window x:Class="StructureHelper.Windows.Shapes.TrapezoidShapeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StructureHelper.Windows.Shapes"
        d:DataContext="{d:DesignInstance local:TrapezoidShapeViewModel}"
        mc:Ignorable="d"
        Title="Trapezoid Shape properties" Height="200" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="22"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Full height"/>
            <TextBox Grid.Column="1" Text="{Binding Height, Converter={StaticResource LengthConverter}, ValidatesOnDataErrors=True}"/>
            <TextBlock Grid.Row="1" Text="Top base"/>
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding TopBase, Converter={StaticResource LengthConverter}, ValidatesOnDataErrors=True}"/>
            <TextBlock Grid.Row="2" Text="Bottom base"/>
            <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding BottomBase, Converter={StaticResource LengthConverter}, ValidatesOnDataErrors=True}"/>
        </Grid>
        <ContentControl Grid.Row="1" ContentTemplate="{StaticResource OkCancelButtons}" Content="{Binding}"/>
    </Grid>
</Window>
