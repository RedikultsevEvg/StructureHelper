<UserControl x:Class="StructureHelper.Infrastructure.UI.DataTemplates.RingShapeTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StructureHelper"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mouseEventTriggers="clr-namespace:StructureHelper.Infrastructure.UI.Triggers.MouseEventTriggers"
             xmlns:dataContexts="clr-namespace:StructureHelper.Infrastructure.UI.DataContexts"
             d:DataContext="{d:DesignInstance dataContexts:RingShapeViewPrimitive}"
             mc:Ignorable="d">
    <Grid>
        <Ellipse Height="{Binding OuterDiameter}" Width="{Binding OuterDiameter}" Fill="DarkGray" Opacity="0.3">
            <Ellipse.ToolTip>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Name: " FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="Bold"/>
                    <TextBlock Grid.Row="1" Text="Material Name: " FontWeight="Bold"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HeadMaterial.Name}" FontWeight="Bold"/>
                    <TextBlock Grid.Row="2" Text="Center X: "/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CenterX, Converter={StaticResource LengthConverter}}"/>
                    <TextBlock Grid.Row="3" Text="Center Y: "/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding CenterY, Converter={StaticResource LengthConverter}}"/>
                    <TextBlock Grid.Row="4" Text="Outer Diameter: "/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding OuterDiameter, Converter={StaticResource LengthConverter}}"/>
                    <TextBlock Grid.Row="5" Text="Inner Diameter: "/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding InnerDiameter, Converter={StaticResource LengthConverter}}"/>
                    <TextBlock Grid.Row="6" Text="Area: "/>
                    <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding Area, Converter={StaticResource AreaConverter}}"/>
                </Grid>
            </Ellipse.ToolTip>
            <Ellipse.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="{Binding CenterX}" Y="{Binding InvertedCenterY}"/>
                </TransformGroup>
            </Ellipse.RenderTransform>
        </Ellipse>
        <Path Style="{StaticResource PolygonStyle}" Tag ="{Binding}" Data="{Binding RingGeometry}">
            <Path.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="{Binding CenterX}" Y="{Binding InvertedCenterY}"/>
                    <TranslateTransform X="{Binding OuterRadius}" Y="{Binding OuterRadius}"/>
                </TransformGroup>
            </Path.RenderTransform>
        </Path>
    </Grid>
</UserControl>
